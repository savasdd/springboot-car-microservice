server:
  host: localhost
  port: 8088

eureka:
  client:
    service-url:
      defaultZone: http://${server.host}:8761/eureka

spring:
  application:
    name: location-service
  banner:
    location: banner.txt
  config:
    import: info.yml